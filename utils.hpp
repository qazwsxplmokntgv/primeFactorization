#pragma once

//there used to be more going on here but i moved it all to more appropriate locations
//still don't know where else to put these though

constexpr size_t panelWidth = 72;
constexpr size_t miniPanelWidth = panelWidth / 2;
